# 2026-06-13 工作日志

## VALUE_QUALITY 策略缺失修复
- 问题：Phase 1 新增的4个因子（VAL_PE_PERCENTILE, VAL_PB_PERCENTILE, PRICE_52W_HIGH_PCT, VAL_FCF_YIELD）只在 RecommendationService 的 VALUE_QUALITY_FACTORS 硬编码列表中使用，但数据库中没有对应的 VALUE_QUALITY 策略记录
- 修复：在 RecommendationService.mapStrategyToProfile() 中添加 VALUE_QUALITY 的代码映射和名称映射

## 删除 StrategyDataInitializer
- 策略已在数据库中定义，StrategyDataInitializer 每次启动检查/初始化纯属冗余
- 删除后策略管理完全依赖数据库，通过前端UI操作
- 无其他代码引用，删除干净
- 注意：已有数据库需要确保 VALUE_QUALITY 策略记录存在

## VALUE_QUALITY 策略已通过 API 创建
- 数据库现有40条策略，但无 VALUE_QUALITY
- 通过 POST /api/strategies 成功创建，id=77
- 包含6个因子：VAL_PE_PERCENTILE(0.25), VAL_PB_PERCENTILE(0.20), PRICE_52W_HIGH_PCT(0.15), VAL_FCF_YIELD(0.20), FIN_EARNINGS_QUALITY(0.10), FIN_NET_PROFIT_YOY(0.10)
- 同时创建了 strategy_seed_data.sql 作为备份（INSERT IGNORE + 唯一索引）
- 发现数据库中已有策略代码是 RECOMMEND_* 前缀（如 RECOMMEND_COMPREHENSIVE, RECOMMEND_EXISTING 等），和 mapStrategyToProfile 中的映射名不一致

## 待办：RecommendationService 硬编码清理 ✅ 已完成
- 现状：getFactorConfig() 优先读数据库 strategy.factorConfigJson，硬编码 PROFILE_FACTORS_MAP 只是兜底
- 硬编码常量：EXISTING_FACTORS / NORMAL_FACTORS / NEW_QUALITY_FACTORS / HOT_FACTORS / COMPREHENSIVE_FACTORS / VALUE_QUALITY_FACTORS
- 行业排除：PROFILE_EXCLUDE_INDUSTRY_MAP 也是硬编码兜底
- mapStrategyToProfile() 映射也是硬编码
- **已全部清理**，详见下方

## RecommendationService 硬编码清理（完成）
### 删除的内容（约140行）
1. **6组硬编码因子配置常量**：EXISTING_FACTORS, NORMAL_FACTORS, NEW_QUALITY_FACTORS, HOT_FACTORS, COMPREHENSIVE_FACTORS, VALUE_QUALITY_FACTORS + PROFILE_FACTORS_MAP 映射表
2. **硬编码行业排除映射**：PROFILE_EXCLUDE_INDUSTRY_MAP（NEW_QUALITY/HOT等策略的行业排除规则）
3. **硬编码申万排除列表**：NEW_QUALITY_EXCLUDE_SW1
4. **策略名称映射方法**：mapStrategyToProfile() + resolveEffectiveProfile()

### 方法改造
| 方法 | 改前 | 改后 |
|---|---|---|
| `getFactorConfig(profile, id)` | 先读DB，失败回退硬编码 | 只读DB strategy.factorConfigJson，无配置则抛异常 |
| `getExcludeIndustries(profile, id)` | DB+硬编码合并 | 只读DB filterConfigJson.excludeIndustries |
| `screenStocks(date, profile, id, ...)` | 传 factorProfile 参数 | 只传 strategyId |
| `generateRecommendations(...)` | factorProfile 参数 | 移除，只保留 strategyId |

### 联动修改的调用方
- RecommendationController：不再读取/传递 factorProfile
- LlmAnalysisController：改为传 strategyId 参数
- ScheduleService：从 extra_config 读 strategyId 而非 factorProfile

### 关键设计决策
- 无 strategyId 时直接抛异常，不再有"默认因子兜底"
- 行业排除中"无行业信息股票"改为通用安全规则（只要配了行业排除就过滤无信息的）

## VALUE_QUALITY 行业排除列表修正
- **核心问题**：stock_info.industry 存的是申万**二级**行业名（91个），而非申万一级
- 旧硬编码 NEW_QUALITY_EXCLUDE_SW1 用申万一级名（14个），8个永远匹配不到（非银金融、煤炭、石油石化、公用事业、交通运输、社会服务、纺织服饰、农林牧渔）
- 用户提供的列表（19个）也有12个不匹配（信托、期货、房地产开发、煤炭开采、石油石化等）
- **已修正**：更新为31个申万二级名，100%匹配 stock_info.industry
- 分类：金融4 + 地产1 + 上游周期9 + 基建2 + 公用事业2 + 交运4 + 消费服务6 + 农林3

## VALUE_QUALITY vs RECOMMEND_NEW_QUALITY 行业排除差异化
- id=74 新质生产力：31个排除（排除所有传统行业，只留新经济）
- id=77 低价优质：16个排除（只排除价值陷阱/纯周期，保留金融/公用事业等"便宜+稳定"行业）
- 保留金融的理由：银行PE 4-6x、PB 0.5x、股息率5%+，完美匹配"低价优质"

## 智能推荐问号 Tooltip 更新
- 文件：frontend/src/pages/recommendation/RecommendationList.jsx
- 从简单的4步流程改为完整的6步管线说明，每步独立一节
- ❶ 行业排除+黑名单 → ❷ 多因子筛选(Top50) → ❸ 市场环境识别(Regime) → ❹ 个股深度分析(Top20) → ❺ Regime-Adaptive动态融合 → ❻ 行业分散化(动态上限+相关性分组)
- Tooltip maxWidth 从 520 调至 560

## trackRecommendationPerformance NPE 修复
- **错误**：`combo.get("strategy_id")` 返回 null → `.longValue()` NPE
- **根因**：stock_recommendation 表中有5条 strategy_id=NULL 的脏数据（2026-06-10，5只银行股），是重构前的残留
- **修复**：
  1. 删除5条脏数据
  2. RecommendationMapper.findRecentStrategyDates() SQL 加 `WHERE strategy_id IS NOT NULL`
  3. RecommendationService.trackRecommendationPerformance() 中3处 `combo.get("strategy_id")` 调用加 null 防御
  4. Stream 中 `.map()` 前加 `.filter(c -> c.get("strategy_id") != null)`

## Phase 3~5 详细讲解 + LLM推理触发修复
- 向用户完整讲解了Phase 3(大模型推理)/Phase 4(分钟线+监控)/Phase 5(持仓闭环)的实现
- **LLM触发修复**: 前端triggerAnalysis传factorProfile(无效)→后端接受的是strategyId,导致null报错
- **LlmAnalysisController**: strategyId参数defaultValue改为77(不再允许null)
- **api/index.js**: triggerAnalysis签名从(factorProfile='VALUE_QUALITY', topN)改为(strategyId=77, topN)
- **LlmAnalysisPage.js**: 新增策略下拉框Select, fetchStrategies需取res.data?.data?.records(IPage分页对象)

## LLM推理因子数据源修复：MySQL → ClickHouse
- **核心问题**：`LlmAnalysisService.getLatestFactorValues()` 查 MySQL `factor_value` 表，但数据全在 ClickHouse `stock.factor_value FINAL`，导致 LLM prompt 全是 N/A
- **ClickHouseFactorValueService.findLatestBySymbol()**：新增方法，按 symbol 查 CH 获取全部因子的 factor_val + rank_value
- **LlmAnalysisService 改造**：
  1. 注入 `ClickHouseFactorValueService`
  2. `getLatestFactorValues()` 改为 CH 优先 → MySQL stock_daily 回退
  3. 使用 `factor_val`（实际值如 PE=15.3）代替 `rank_value`（排名0.65），LLM 需要真实数值
  4. 新增 `stripSuffix()` 处理 stockCode 后缀：CH 用 `600027.SH`，MySQL 用 `600027`
  5. `getIndustry()` 和 `getFinancialData()` 也加了 `stripSuffix()` 修复
- **修复编译错误**：`getOrDefault("VAL_PE_TTM", -1)` → `-1.0`，`np.abs()` → `Math.abs(np)`

## LLM因子数据N/A深度修复（第二版）
### 根因排查发现4个问题
1. **CH factor_value 存在两种 symbol 格式**：
   - 纯代码（000526）：存94个主因子（RSI/MACD/ROE/PE等）
   - 带后缀（000526.SZ）：仅存3个Python特殊因子（PE分位/PB分位/52周回撤）
   - 之前只查带后缀格式，只拿到3个因子！
2. **MySQL stock_daily 表完全为空**（0行），PE/PB 回退永远失败
3. **MySQL 所有表用 `code` 列名**（非 `stock_code`），之前SQL写错列名
4. **stock_financial_indicator 无 `operating_cash_flow`/`net_profit` 列**，实际用 `net_operate_cf` + `operating_cf_to_np`
5. **CH stock_daily 有完整数据**（717万行/5490股），含 PE/PB/high_price

### 修改内容
**ClickHouseFactorValueService.java：**
- `findLatestBySymbol()` 改为双格式查询：先查纯代码(94因子) → 再查带后缀(3因子) → 合并
- 新增 `findStockDailyLatest(code)` 查 CH stock_daily 获取 PE/PB/价格

**LlmAnalysisService.java：**
- `getLatestFactorValues()` MySQL回退改为 CH stock_daily 回退（stock_daily表有数据）
- `getFinancialData()` SQL: `stock_code` → `code`, `operating_cash_flow` → `net_operate_cf`, `net_profit` → `operating_cf_to_np`
- `getIndustry()`: `stock_code` → `code`

### 修复效果（以000526.SZ为例）
| 指标 | 修复前 | 修复后 |
|---|---|---|
| 总因子数 | 3 | 99 |
| PE(TTM) | N/A | 15.03 |
| PB | N/A | 3.06 |
| RSI14 | N/A | 44.57 |
| MACD | N/A | -2.62 |
| VOL20 | N/A | 7.95 |
| 52周回撤 | N/A | -54.59% |
| LLM关键覆盖率 | ~0% | 62%（10/16）|
| 剩余缺失 | 全部缺失 | 仅VAL_DIVIDEND_YIELD/FCF_YIELD/FIN_EARNINGS_QUALITY等（该股票未被引擎计算这些因子）|

## LLM分析页面重复记录修复
- **问题**：`findByDate` 返回今日所有记录，多次触发导致同一股票出现多条
- **修复**：SQL改为 GROUP BY stock_code 取最新条（子查询 MAX(created_at) JOIN）
- **确认**：最新批次id=21~29全部有真实数据，PE/ROE/现金流等均正确；北交所股票也有完整因子

## LLM JSON截断修复
- **问题**：`max-tokens: 2048` 不够用，DeepSeek输出被截断，JSON不完整导致解析失败
- **修复**：
  1. `max-tokens` 从 2048 → 4096
  2. `LlmService` 新增 `repairTruncatedJson()` 方法：检测截断→补齐缺失引号/括号→验证修复结果
  3. `chatAsJson()` 增加二次解析：首次失败→尝试截断修复→再解析

## 北交所股票LLM分析N/A根因深度排查与修复（第三版）
### 问题现象
- 利尔达(920249.BJ): 有部分数据(ROE=1.9%)但买入价/止损/目标价=N/A
- 舜宇精工(920906.BJ): 全部N/A，"基本面和估值数据全部缺失"

### 根因分析（4层）
1. **VAL_*/FIN_* 因子按季度计算**：最新仅到2026-03-31(Q1)，Q2(06-30)未到
   - BJ股票VAL_PE_TTM最后计算日: 2026-03-31（244只BJ有数据，598只中约一半缺失）
   - BJ股票FIN_ROE最后计算日: 2026-03-31（298只BJ有数据）
   - 技术因子(RSI/MACD等)每日更新正常（94因子/股/天）

2. **纯代码查询遗漏后缀格式**：StockRecommendation.stockCode是纯代码("920906")
   - findLatestBySymbol("920906") → stripSuffix还是"920906" → 两次查同一格式
   - 永远不会查询"920906.BJ"格式 → 丢失3个Python特殊因子(PRICE_52W_HIGH_PCT/VAL_PE_PERCENTILE/VAL_PB_PERCENTILE)

3. **旧记录未被覆盖**：22:41旧记录(N/A)仍在DB中，23:08新批次未包含920906
4. **CH stock_daily和MySQL财务表均有数据**：PE/PB/ROE等均可从fallback获取

### 修复内容
**LlmAnalysisService.getLatestFactorValues() 三级fallback重构：**
1. CH factor_value 纯代码格式 → 94技术因子
2. **新增**: 查stock_info获取market后缀 → 查带后缀格式(.BJ/.SH/.SZ) → 补充3个特殊因子
3. CH stock_daily → 补齐PE/PB/52周回撤（5490股全覆盖）
4. 新增 `lookupSuffixedCode(pureCode)` 方法：SQL查stock_info获取market后缀

### 数据源覆盖确认
| 数据源 | 920906数据 | 920249数据 |
|---|---|---|
| CH factor_value (纯代码) | 94技术因子 | 94技术因子 |
| CH factor_value (.BJ后缀) | PRICE_52W_HIGH_PCT=-53.96, PE/PB分位=0% | 同上 |
| CH stock_daily | PE=43.83, PB=2.26, close=19.76 | PE=89.85, PB=4.91, close=9.02 |
| MySQL financial_indicator | ROE=0.07%, 净利润+124.6%, 负债率55%, 毛利率18% | ROE=1.9%, 净利润+397%, 负债率65%, 毛利率15% |

### 操作要求
- ✅ 代码已编译通过(mvn compile)
- ⚠️ 需重启后端服务加载新class
- ⚠️ 需重新触发LLM分析（删除今日旧记录或重新推荐）

## LLM价格字段N/A修复（第四版：双层保障）
### 问题
4只北交所股票（利尔达/方大新材/威博液压/坤博精工）的**买入价区间/止损价/目标价**显示N/A

### 根因分析（查DB raw_response确认）
| 股票 | 推荐 | LLM返回值 | 原因 |
|------|------|----------|------|
| 利尔达 920249 | SKIP | buy={0,0}, stop=0, target=0 | LLM认为不该买，返回0 |
| 方大新材 920163 | WATCH | buy={null,null}, stop=null, target=null | LLM遗漏填写 |
| 威博液压 920245 | SKIP | buy={null,null}, stop=null, target=null | 同上 |
| 坤博精工 920570 | SKIP | buy={0,0}, stop=0, target=0 | 同上 |

### 修复方案（双层保障）
**第一层 - Prompt强化**：
- 在USER_PROMPT_TEMPLATE末尾添加【重要约束】段落：
  - 明确声明buy_price_range/stop_loss/target_price为**必填项**
  - 按推荐类型说明计算逻辑（BUY/WATCH/SKIP分别如何定价）
  - 禁止填写0/null/省略

**第二层 - 代码Fallback兜底**（`parseAnalysis()`方法重构）：
1. 新增 `fetchCurrentPrice(stockCode)` → 从CH stock_daily获取最新收盘价
2. 新增 `parsePriceWithFallback(node, defaultValue, currentPrice)` → 解析JSON价格节点
   - null/缺失/0 → 使用defaultValue
   - 异常高值(>10倍) → 警告+使用defaultValue
3. fallback默认值策略：
   - buy_low = 当前价 × 97%（略低于现价）
   - buy_high = 当前价 × 101%（略高于现价）
   - stop_loss = 当前价 × 92%（-8%止损）
   - target_price: BUY=+20%, WATCH=+15%, SKIP=+5%

### Fallback效果预览（基于当前收盘价）
| 股票 | 收盘价 | 买入区间 | 止损价 | 目标价 |
|------|--------|---------|--------|--------|
| 利尔达 9.02 | 9.02 | 8.75~9.11 | 8.30 | 9.47 |
| 方大新材 10.34 | 10.34 | 10.03~10.44 | 9.51 | 11.89 |
| 威博液压 14.93 | 14.48~15.08 | 13.74 | 15.68 |
| 坤博精工 26.50 | 25.71~26.77 | 24.38 | 27.83 |

## 前端修复：选股工具菜单不展开
- **问题**：在 `/llm`、`/recommendation`、`/watchlist`、`/positions` 页面时，左侧"选股工具"菜单不展开
- **原因**：App.js 中 `openKeys` 初始化只匹配 `path === '/screen'`，遗漏了同组其他4个子路由
- **修复**：改为 `path.startsWith('/screen') || path.startsWith('/recommendation') || ...`

## LLM Prompt %转义修复
- **错误**：`UnknownFormatConversionException: Conversion = '以'`
- **原因**：新增的Prompt约束文本 `-8%以内` 被 String.format() 当作格式化占位符
- **修复**：`%以内` → `%%以内`（标准转义）
