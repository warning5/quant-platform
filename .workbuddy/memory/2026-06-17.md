# 2026-06-17 工作日志

## 修复
- update_news_data.py: `_load_model_local()` 的 `from_pretrained()` 加 `local_files_only=True`，解决新版 transformers 拒绝 HF cache snapshot 路径（含`--`）的 HFValidationError
- update_news_data.py: 本地NLP模型缓存已不存在，改为**优雅降级**：`_init_sentiment()`/`_init_refresh()` 检查 `os.path.isdir()`，缺失时标记 `KW_ONLY`，`classify_sentiment()` 关键词未命中返回 `(0.5, neutral)`，`run_refresh()` neutral 记录保持默认评分
- 依赖声明更新：transformers/torch 改为可选
- update_consensus_estimate.py: 并发优化（4线程+每线程独立MySQL连接），去掉逐任务0.2s延迟改为一次性提交
- update_consensus_estimate.py: 加 `_clean_decimal()` 清洗同花顺 `'-'` 占位符 → None，修复 DECIMAL 列报错
- RecommendationList.jsx: ~~复盘区域新增全量明细表~~（已删除，用户要的是趋势图+复盘）
- RecommendationList.jsx 修复复盘不显示：
  - **趋势图**：`trackedBatches.length >= 2` → `>= 1`（策略77只有1个已追踪批次时不显示）
  - **最佳/最差复盘**：选中日期未追踪时（nextDayReturn全null），自动回退到最近已追踪批次显示，标题加橙色Tag提示
- **北交所股票混入A股策略问题修复**：
  - 根因：策略77（低价优质）2026-06-12的20只推荐中15只(75%)是BJ股票（晶赛科技、欧康医药等），filterConfigJson缺少市场维度过滤
  - StockScreenService.applyFilterConfig() 新增 `excludeMarkets` 支持：`["BJ"]` 排除code以8/9开头的北交所股票
  - 批量更新9个A股策略(56,72,73,74,75,76,77,78,80,82) 的 filter_config_json 加上 `"excludeMarkets":["BJ"]`
  - 未改的策略：79(涨停板)/81(市场情绪)/83(事件驱动) 属于全市场策略
  - **回滚**：用户明确说不排除北交所，已将全部9个策略的 `excludeMarkets: ["BJ"]` 回滚删除（JSON_REMOVE）
- MonitorPage.js: "未启动"/"运行中" Tag 加 Tooltip 提示（非交易时段或服务刚重启）
- 排查盘中监控 dataDate=06-12 而非 06-16：根因 `stock_recommendation` 最新推荐日期就是06-12，之后未生成新推荐
- 排查每日推荐调度未执行原因：`data_schedule_config` 中 `GLOBAL.enabled=0`（全局调度开关关闭），`refreshFromDb()` 直接 return 不注册任何任务，DAILY_RECOMMENDATION 的 cron `0 15 7 * * ?` 从未触发（last_run_time=null）
- ScheduledTasks.jsx: 修复"修改"弹窗卡死问题：
  - **根因**：`getNextRunTimes()` 用逐秒暴力遍历计算cron下次执行时间，对 `0 15 7 * * ?` 需迭代5.6万次阻塞主线程
  - **算法重写**：逐秒遍历 → 字段级跳跃定位（秒/分/时/月二分查找+进位，日逐日+星期过滤），复杂度从 O(N秒) 降到 O(10~30)
  - **异步化**：`useMemo` 同步计算 → `setTimeout(0)` + `useState` 异步渲染，加"计算中..."Loading提示
- ScheduledTasks.jsx: DAILY_RECOMMENDATION 调度编辑器增强：
  - **策略多选**：编辑器"任务配置"Tab 中，当 taskKey=DAILY_RECOMMENDATION 时显示策略多选器（从 /api/strategies 获取列表），支持选择任意策略组合
  - **额外配置**：每策略推荐数(topN)、置信度控制开关、同步监控开关
  - **parseExtraConfig/stringifyExtraConfig 扩展**：支持 strategyIds/topN/enableConfidenceControl/syncToWatchlist 字段，strategyIds 非空时才写入
  - DB: `data_schedule_config` 中 DAILY_RECOMMENDATION 的 task_name 从 "每日自动推荐(6策略)" → "每日自动推荐"
  - ScheduleConfigController.java: PUT 接口新增 task_name 字段更新支持
- 前端构建：项目使用 Vite（非 react-scripts），`npm run build` = `vite build`，输出到 build/ 目录
- 前后端编译验证均通过（Maven BUILD SUCCESS + Vite built in 16.89s）
- ScheduledTasks.jsx: 修复策略选择器只显示ID数字不显示名称：
  - **根因**：axios 拦截器已解包返回 `res.data.data`（IPage对象 `{records, total}`），但代码仍用 `res?.data?.records` 取值 → 取到 undefined → allStrategies=[] → Select 无 options 但有 strategyIds 值 → 只能显示原始 ID tag
  - **修复**：`res?.data?.records` → `res?.records`，并加 `size=100` 参数确保获取全部13个策略
  - esbuild 不支持三元嵌套在 || 链中（`a || (b ? c : d) || e`），拆分为两行赋值
- **axios 拦截器约定**：api.get() 返回的 res 已经是 `response.data.data`（拦截器第33行解包），不要再用 res.data 访问
- EventTag 枚举类：从 NewsEventParser 硬编码字符串抽取为 `com.quant.platform.stock.analysis.domain.EventTag` 枚举
  - 12个标签（BUYBACK/INCREASE/DECREASE/EARN_PRE/EARN_WARN/EARN_BEAT/EARN_MISS/RESTRUCT/UNLOCK/INCENTIVE/DIVIDEND/OTHER）
  - 内嵌 Direction 枚举（BULLISH/BEARISH/NEUTRAL）
  - 静态方法：fromCode()、isValidCode()、getBullishCodes()、getBearishCodes()、toPromptText()（生成LLM Prompt标签说明）
  - NewsEventParser 重构：删除 VALID_TAGS/BULLISH_TAGS/BEARISH_TAGS 硬编码集合，改用 EventTag 静态方法
- ScheduleConfigController 触发 DAILY_RECOMMENDATION 报 "未知的任务类型"：
  - **根因**：`triggerTask()` 用 `buildRequestFromKey()` 构建请求走 DataUpdateService，但 `buildRequestFromKey()` 的 switch 里没有 DAILY_RECOMMENDATION/RECOMMENDATION_TRACK 的 case → default 抛异常
  - **修复**：`triggerTask()` 中对 DAILY_RECOMMENDATION/RECOMMENDATION_TRACK 走 ScheduleService.executeTask() 异步执行，不走 DataUpdateService
  - ScheduleService.executeTask() 从 private 改为 public
- antd message 静态方法警告全量修复：12个前端文件 `import { message } from 'antd'` → `import { message } from '../../utils/messageUtil'`（通过 AntdApp context bridge）
- ScheduledTasks.jsx: 修复"触发"按钮提示红色错误"任务已异步提交执行"：
  - **根因**：DAILY_RECOMMENDATION 触发返回 `{taskKey, status, message}` 无 `taskId` 字段，但前端判断条件是 `res.taskId`（仅普通数据更新任务有此字段），走到 else 分支用 `message.error()` 展示成功信息
  - **修复**：判断条件从 `res.taskId` 改为 `res.taskKey || res.taskId || res.status === 'RUNNING'`，且 success 提示使用后端返回的 `res?.message` 更准确
- 删除 syncToWatchlist 功能（"我的收藏"/stock_watchlist 表不存在，功能无实际作用）：
  - ScheduleService.java: 删除 `syncToWatchlist` 变量、`executeDailyRecommendation()` 中的同步逻辑块、整个 `syncRecommendationsToWatchlist()` 方法
  - ScheduledTasks.jsx: 删除 `syncToWatchlist` state、Switch UI、`parseExtraConfig`/`stringifyExtraConfig`/`buildExtraConfig` 中的相关字段及依赖数组
  - DB: 从 DAILY_RECOMMENDATION 的 extra_config JSON 中移除 `syncToWatchlist` 字段
  - 临时文件 `create_watchlist_table.py` 已删除
  - 前后端构建验证通过（Maven compile + Vite build）
- IntradayMonitorService 盘中监控数据日期修复：
  - **根因**：`getLatestDataDate()` 先查 llm_analysis 日期(06-14)，找到后就不再查 stock_recommendation 日期(06-16)，导致监控用旧日期加载数据
  - **修复1**：`getLatestDataDate()` 改为取两个数据源最大值（不再只取第一个非空值）
  - **修复2**：`loadTargetPrices()` 改为两个数据源各用自己的最新日期查询（LLM用06-14查10条，推荐用06-16查4条BUY），显示日期取两者最大值
  - Maven compile 通过