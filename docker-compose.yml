version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quant-platform
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=quant_platform
      - MYSQL_USERNAME=quant
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-quant123}
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - CLICKHOUSE_DATABASE=stock
      - CLICKHOUSE_USERNAME=default
      - CLICKHOUSE_PASSWORD=${CLICKHOUSE_PASSWORD:-}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-quantRedis123}
    depends_on:
      mysql:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - quant-net

  mysql:
    image: mysql:8.0
    container_name: quant-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-root123}
      - MYSQL_DATABASE=quant_platform
      - MYSQL_USER=quant
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-quant123}
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - quant-net

  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    container_name: quant-clickhouse
    ports:
      - "8123:8123"
      - "9000:9000"
    volumes:
      - clickhouse-data:/var/lib/clickhouse
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - quant-net

  redis:
    image: redis:7-alpine
    container_name: quant-redis
    # 设置访问密码，避免未授权访问（默认密码仅兜底，生产须覆盖 REDIS_PASSWORD）
    command: redis-server --requirepass ${REDIS_PASSWORD:-quantRedis123}
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:-quantRedis123}
    # 不向宿主机暴露端口，仅容器内通过服务名 redis:6379 互访，消除公网未授权访问风险
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $$REDIS_PASSWORD ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - quant-net

volumes:
  mysql-data:
  clickhouse-data:
  redis-data:

networks:
  quant-net:
    driver: bridge
