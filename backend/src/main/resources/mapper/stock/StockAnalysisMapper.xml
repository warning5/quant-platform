<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quant.platform.stock.analysis.mapper.StockAnalysisMapper">
    
    <!-- 查询机构调研统计（最近N天） -->
    <select id="selectSurveyStats" resultType="java.util.Map">
        SELECT 
            COUNT(*) as survey_count,
            AVG(survey_count) as avg_survey_count
        FROM stock_sentiment_survey
        WHERE code = #{code}
          AND max_date >= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
    </select>
    
    <!-- 查询涨跌停池统计（最近N天） -->
    <select id="selectZtPoolStats" resultType="java.util.Map">
        SELECT 
            SUM(CASE WHEN zt_type = 'zt' THEN 1 ELSE 0 END) as limit_up_count,
            SUM(CASE WHEN zt_type = 'zbgc' THEN 1 ELSE 0 END) as broken_count,
            AVG(strength) as avg_strength
        FROM stock_sentiment_zt
        WHERE code = #{code}
          AND trade_date >= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
    </select>
    
    <!-- 查询基本面数据：pe_ttm/pb 从 CH stock_daily 取（MySQL stock_daily 为空），其余从 stock_financial_indicator 取 -->
    <select id="selectFundamentalSignal" resultType="com.quant.platform.stock.analysis.domain.FundamentalSignal">
        SELECT
            fi.roe as roe,
            fi.revenue_yoy as revenueYoy,
            fi.net_profit_yoy as netProfitYoy,
            fi.gross_profit_margin as grossMargin,
            fi.debt_to_asset_ratio as debtRatio
        FROM stock_financial_indicator fi
        WHERE fi.code = #{code}
          AND fi.end_date = (SELECT MAX(end_date) FROM stock_financial_indicator WHERE code = #{code})
        LIMIT 1
    </select>
    
    <!-- 查询事件面数据 -->
    <select id="selectSentimentSignal" resultType="com.quant.platform.stock.analysis.domain.SentimentSignal">
        SELECT
            -- 近10日涨停天数（只统计zt_type='zt'，按日期去重）
            COALESCE((
                SELECT COUNT(DISTINCT trade_date) FROM stock_sentiment_zt
                WHERE code = #{code}
                  AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 10 DAY)
                  AND zt_type = 'zt'
            ), 0) as limitUpDays,
            0 as limitUpStrength,
            -- 近20日炸板次数
            (SELECT COUNT(DISTINCT trade_date) FROM stock_sentiment_zt
             WHERE code = #{code} AND zt_type = 'zbgc'
             AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 20 DAY)) as brokenLimitUpCount20,
            -- 炸板率 = 炸板天数 / (涨停天数+炸板天数) * 100（只看涨停相关）
            COALESCE((
                SELECT CASE WHEN zt_total > 0 THEN ROUND(zbgc_days * 100.0 / zt_total, 1) ELSE 0 END
                FROM (
                    SELECT
                        (SELECT COUNT(DISTINCT trade_date) FROM stock_sentiment_zt
                         WHERE code = #{code} AND zt_type IN ('zt', 'zbgc')
                         AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 20 DAY)) as zt_total,
                        (SELECT COUNT(DISTINCT trade_date) FROM stock_sentiment_zt
                         WHERE code = #{code} AND zt_type = 'zbgc'
                         AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 20 DAY)) as zbgc_days
                ) t
            ), 0.0) as brokenLimitUpRate,
            FALSE as isStrongStock,
            -- 龙虎榜净买入（近20日）
            (SELECT COALESCE(SUM(net_amount), 0) FROM stock_sentiment_lhb
             WHERE code = #{code}
               AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 20 DAY)) as lhbNetAmount,
            -- 龙虎榜上榜次数（近20日）
            (SELECT COUNT(*) FROM stock_sentiment_lhb
             WHERE code = #{code}
               AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 20 DAY)) as lhbAppearCount,
            -- 正面公告数（近30日：回购/增持/业绩预增/股权激励）
            (SELECT COUNT(*) FROM stock_sentiment_notice
             WHERE code = #{code}
               AND notice_date >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
               AND (title LIKE '%回购%' OR title LIKE '%增持%'
                    OR title LIKE '%预增%' OR title LIKE '%股权激励%'
                    OR title LIKE '%业绩快报%' OR title LIKE '%业绩预告%预增%')) as noticePositiveCount,
            -- 负面公告数（近30日：减持/定增/业绩预降/被调查）
            (SELECT COUNT(*) FROM stock_sentiment_notice
             WHERE code = #{code}
               AND notice_date >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
               AND (title LIKE '%减持%' OR title LIKE '%定增%'
                    OR title LIKE '%预降%' OR title LIKE '%预亏%'
                    OR title LIKE '%被调查%' OR title LIKE '%立案%'
                    OR title LIKE '%行政处罚%' OR title LIKE '%退市%')) as noticeNegativeCount
        FROM dual
    </select>
    
    <!-- 查询股票基本信息（仅从 stock_info 取名称+行业+总市值，价格从 CH 获取） -->
    <select id="selectStockInfo" resultType="java.util.Map">
        SELECT
            si.name,
            si.industry,
            si.total_market_cap
        FROM stock_info si
        WHERE si.code = #{code}
        LIMIT 1
    </select>

    <!-- 查询研报数据（最近90天） -->
    <select id="selectResearchSignal" resultType="com.quant.platform.stock.analysis.domain.ResearchSignal">
        SELECT
            (SELECT rating FROM stock_research_report 
             WHERE code = #{code} AND rating IS NOT NULL AND rating != ''
             ORDER BY report_date DESC LIMIT 1) as latestRating,
            (SELECT COUNT(*) FROM stock_research_report 
             WHERE code = #{code} 
             AND report_date >= DATE_SUB(CURDATE(), INTERVAL 90 DAY)) as reportCount
        FROM dual
    </select>

    <!-- 查询研报明细列表（最近5条） -->
    <select id="selectRecentResearchReports" resultType="java.util.Map">
        SELECT
            rating,
            institution,
            report_date as reportDate,
            report_title as reportTitle,
            eps_forecast as epsForecast
        FROM stock_research_report
        WHERE code = #{code}
        ORDER BY report_date DESC
        LIMIT 5
    </select>

    <!-- 查询研报明细列表（含EPS预测，指定条数） -->
    <select id="selectRecentReportsWithEps" resultType="java.util.Map">
        SELECT
            rating,
            institution,
            report_date as reportDate,
            report_title as reportTitle,
            eps_forecast as epsForecast,
            pdf_url as pdfUrl
        FROM stock_research_report
        WHERE code = #{code}
        ORDER BY report_date DESC
        LIMIT #{limit}
    </select>

    <!-- 评级趋势（近6个月） -->
    <select id="selectRatingTrend" resultType="java.util.Map">
        SELECT
            DATE_FORMAT(report_date, '%Y-%m') AS month,
            IFNULL(rating, '无评级') AS rating,
            COUNT(*) AS cnt
        FROM stock_research_report
        WHERE code = #{code}
          AND report_date >= DATE_SUB(CURDATE(), INTERVAL 6 MONTH)
        GROUP BY month, rating
        ORDER BY month ASC
    </select>

    <!-- 研报数量月度趋势（近6个月） -->
    <select id="selectReportCountByMonth" resultType="java.util.Map">
        SELECT
            DATE_FORMAT(report_date, '%Y-%m') AS month,
            COUNT(*) AS cnt
        FROM stock_research_report
        WHERE code = #{code}
          AND report_date >= DATE_SUB(CURDATE(), INTERVAL 6 MONTH)
        GROUP BY month
        ORDER BY month ASC
    </select>

    <!-- 覆盖机构列表（去重） -->
    <select id="selectCoverageInstitutions" resultType="java.util.Map">
        SELECT
            institution,
            MIN(report_date) AS first_date,
            COUNT(*) AS report_count
        FROM stock_research_report
        WHERE code = #{code}
          AND institution IS NOT NULL
          AND institution != ''
        GROUP BY institution
        ORDER BY report_count DESC
        LIMIT 20
    </select>

    <!-- 首次覆盖日期 -->
    <select id="selectFirstCoverageDate" resultType="String">
        SELECT MIN(report_date) FROM stock_research_report
        WHERE code = #{code} AND report_date IS NOT NULL
    </select>

    <!-- 股票联想搜索（按代码或名称模糊匹配） -->
    <select id="searchStocks" resultType="java.util.Map">
        SELECT code, name, market
        FROM stock_info
        WHERE code LIKE CONCAT('%', #{keyword}, '%')
           OR name LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY code ASC
        LIMIT 20
    </select>

    <!-- 同业对比：获取同行业股票基本信息（PE/PB/市值/ROE等） -->
    <select id="selectIndustryPeers" resultType="java.util.Map">
        SELECT
            si.code,
            si.name,
            si.market,
            si.industry,
            si.pe_ttm as peTtm,
            si.pb as pb,
            si.total_market_cap as totalMarketCap,
            si.float_market_cap as floatMarketCap
        FROM stock_info si
        WHERE si.industry = #{industry}
          AND si.code NOT LIKE '%.BJ'
        ORDER BY si.total_market_cap DESC
    </select>
    <select id="selectIndustryRanking" resultType="java.util.Map">
        SELECT
            si.industry,
            COUNT(*) as stockCount,
            AVG(sd.change_percent) as avgChangePct
        FROM stock_info si
          LEFT JOIN (
            SELECT code, change_percent FROM stock_daily
            WHERE trade_date = (SELECT MAX(trade_date) FROM stock_daily)
          ) sd ON sd.code = si.code
        WHERE si.industry IS NOT NULL AND si.industry != ''
          AND si.market NOT IN ('BJ','北交所')
        GROUP BY si.industry
        ORDER BY avgChangePct DESC
        LIMIT 30
    </select>

    <!-- 概念板块涨跌排行 -->
    <select id="selectConceptRanking" resultType="java.util.Map">
        SELECT
            sc.concept_name as conceptName,
            COUNT(*) as stockCount,
            AVG(sd.change_percent) as avgChangePct
        FROM stock_concept sc
          LEFT JOIN (
            SELECT code, change_percent FROM stock_daily
            WHERE trade_date = (SELECT MAX(trade_date) FROM stock_daily)
          ) sd ON sd.code = sc.code
        GROUP BY sc.concept_name
        ORDER BY avgChangePct DESC
        LIMIT 30
    </select>

    <!-- 查询所有概念板块股票映射 -->
    <select id="selectAllConcepts" resultType="java.util.Map">
        SELECT
            sc.concept_name as conceptName,
            sc.code
        FROM stock_concept sc
        ORDER BY sc.concept_name, sc.code
    </select>

    <!-- 大盘蓝筹：融资余额变化百分比（最新 vs 5日前） -->
    <select id="selectMarginChangePct" resultType="java.math.BigDecimal">
        SELECT
            (b.latest_balance - b.past_balance) / b.past_balance * 100 as chgPct
        FROM (
            SELECT
                (SELECT margin_balance FROM stock_sentiment_margin_detail WHERE code = #{code} ORDER BY trade_date DESC LIMIT 1) as latest_balance,
                (SELECT margin_balance FROM stock_sentiment_margin_detail WHERE code = #{code} ORDER BY trade_date DESC LIMIT 1 OFFSET 4) as past_balance
        ) b
        WHERE b.past_balance IS NOT NULL
          AND b.past_balance != 0
    </select>

    <!-- 大盘蓝筹：龙虎榜机构净买入（最近20日） -->
    <select id="selectLhbInstitutionNet" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(net_inst_amt), 0) as netAmount
        FROM stock_sentiment_lhb_inst
        WHERE code = #{code}
          AND trade_date >= DATE_SUB(CURDATE(), INTERVAL 20 DAY)
    </select>

    <!-- 大盘蓝筹：机构调研热度（最近90天调研次数，替代股东户数） -->
    <select id="selectHolderChangePct" resultType="java.math.BigDecimal">
        SELECT COUNT(*) as surveyCount
        FROM stock_sentiment_survey
        WHERE code = #{code}
          AND meeting_date >= DATE_SUB(CURDATE(), INTERVAL 90 DAY)
    </select>

</mapper>
